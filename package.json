{
  "name": "coffee-milk-beer",
  "version": "1.0.0",
  "description": "Geospatial visualization project with MapLibre GL JS, Tailwind CSS, and OSMnx isochrones",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "dev:all": "concurrently -n TAILWIND,VITE -c blue,green \"npm:tailwind:watch\" \"npm:dev\"",
    "build": "vite build",
    "preview": "vite preview",
    "tailwind:watch": "tailwindcss -i ./src/css/tailwind.css -o ./dist/tailwind.css --watch",
    "tailwind:build": "tailwindcss -i ./src/css/tailwind.css -o ./dist/tailwind.css --minify",
    "build:site": "npm run tailwind:build && npm run build",
    "build:data": "uv run python tools/generate_isochrones.py && uv run python tools/convert_to_pmtiles.py",
    "build:all": "npm run build:data && npm run build:site"
  },
  "dependencies": {
    "maplibre-gl": "^3.6.0",
    "pmtiles": "^3.0.0"
  },
  "devDependencies": {
    "vite": "^5.0.0",
    "tailwindcss": "^3.4.0",
    "autoprefixer": "^10.4.16",
    "postcss": "^8.4.32",
    "concurrently": "^8.2.2"
  },
  "keywords": [
    "geospatial",
    "mapping",
    "maplibre",
    "tailwind",
    "osmnx",
    "isochrones"
  ],
  "author": "Your Name",
  "license": "MIT"
}
